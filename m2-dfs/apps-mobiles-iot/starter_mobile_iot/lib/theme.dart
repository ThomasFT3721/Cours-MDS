import 'package:flutter/material.dart';

/// Le thème du module : un seul endroit pour les couleurs et les styles.
/// À explorer (et personnaliser) une fois le jalon 0 passé.
final ThemeData themeDuModule = ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.teal),
  useMaterial3: true,
);
